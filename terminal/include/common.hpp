#pragma once 

// libraries that are used in all/most files
#include <iostream>
#include <string>
#include <vector>

// don't use the whole <std> namespace, just what's needed
using std::string;
using std::vector;
using std::endl;
using std::ostream;

// simple typedef
typedef unsigned int uint;

// one byte unsigned integer
typedef unsigned char small_int;